#include <stdio.h>

// Prototypes
void initialize_grid(int size, int arr[size][size]);

int main(void) {
  int size = 0;
  printf("Size for simulation: ");
  scanf("%d", &size);

  int grid[size][size];
}
